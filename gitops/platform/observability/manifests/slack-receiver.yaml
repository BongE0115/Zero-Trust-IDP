apiVersion: v1
kind: Service
metadata:
  name: slack-receiver-svc
  namespace: kafka-poc
spec:
  type: NodePort
  selector:
    app: slack-receiver
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30082 