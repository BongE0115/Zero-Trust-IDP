apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka-operator-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: 'https://github.com/BongE0115/Zero-Trust-IDP.git'
    targetRevision: jy # 프랜치에 따라 수정하면 됨. 
    path: gitops/apps/kafka-operator/manifests
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: kafka
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true